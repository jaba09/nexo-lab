# Opcional: usa una ruta absoluta para cambiar la ubicación predeterminada.
# NEXO_LAB_DB_PATH=/ruta/absoluta/nexo-lab.sqlite
NEXO_LAB_BOOTSTRAP_EMAIL=jablasal@unizar.es
# Usa una contraseña inicial de 12 caracteres o más. No la guardes en Git.
NEXO_LAB_BOOTSTRAP_PASSWORD=
SMTP_HOST=smtp.unizar.es
SMTP_PORT=587
SMTP_USER=jablasal@unizar.es
# Introduce la contraseña solo en el entorno local o en los secretos de Render.
SMTP_PASSWORD=
EMAIL_FROM="Nexo Lab <jablasal@unizar.es>"
NEXO_LAB_PUBLIC_URL=https://nexo-lab.onrender.com
PORT=3000
